'use strict';

/*
 * APELLIDO, NOMBRE | APELLIDO, NOMBRE
 */
//Declaro las variables
let disco, artista, cod_disco, cancion, duracion;
/**
 * Llamada desde un boton. Pide los datos para un disco.
 */
function cargar() {
    // TODO:
    disco=prompt("ingrese nombre del disco: ");
    artista=prompt("ingrese el nombre del artista / banda: ");
    cod_disco=Number(prompt("ingrese codigo del disco: "));
    cancion=prompt("ingrese el nombre de la cancion: ");
    duracion=prompt("ingrese la duracion de la pista: ");
       
}

/**
 * Llamada desde un boton. Muestra todos los discos disponibles.
 */
function mostrar() {
    // TODO
};
